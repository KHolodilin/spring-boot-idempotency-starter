<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.kholodilin</groupId>
        <artifactId>spring-boot-idempotency-starter-parent</artifactId>
        <version>0.3.4-SNAPSHOT</version>
    </parent>

    <artifactId>idempotency-distributed-cache-redis</artifactId>
    <name>Idempotency Distributed Cache Redis</name>
    <description>DistributedCache implementation backed by Redis: shared cache between application instances.</description>
    <url>https://github.com/KHolodilin/spring-boot-idempotency-starter</url>

    <dependencies>
        <dependency>
            <groupId>com.kholodilin</groupId>
            <artifactId>idempotency-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.data</groupId>
            <artifactId>spring-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>io.lettuce</groupId>
            <artifactId>lettuce-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>com.kholodilin</groupId>
            <artifactId>idempotency-local-cache-caffeine</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <!--
          Reactor-only test fakes. activeByDefault is turned off when this module's
          `release` profile is selected via `mvn -P release` (Central publish).
        -->
        <profile>
            <id>idempotency-core-test-jar</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>com.kholodilin</groupId>
                    <artifactId>idempotency-core</artifactId>
                    <type>test-jar</type>
                    <scope>test</scope>
                </dependency>
            </dependencies>
        </profile>
        <!-- Same id as parent release profile: activates with -P release and disables activeByDefault. -->
        <profile>
            <id>release</id>
        </profile>
        <!-- Same id as parent github-packages profile: skip reactor-only test-jar on publish. -->
        <profile>
            <id>github-packages</id>
        </profile>
    </profiles>
</project>

